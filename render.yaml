services:
  - type: web
    name: bill-reader-system
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bill-db
          property: connectionString

databases:
  - name: bill-db